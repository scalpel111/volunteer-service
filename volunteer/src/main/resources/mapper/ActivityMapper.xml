<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.volunteer.mapper.ActivityMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.volunteer.entity.Activity">
        <id column="activity_id" property="activityId" />
        <result column="theme" property="theme" />
        <result column="start_time" property="startTime" />
        <result column="end_time" property="endTime" />
        <result column="address" property="address" />
        <result column="require" property="require" />
        <result column="discription" property="discription" />
        <result column="recruit_number" property="recruitNumber" />
        <result column="tip" property="tip" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        activity_id, theme, start_time, end_time, address, require, discription, recruit_number, tip
    </sql>

</mapper>
