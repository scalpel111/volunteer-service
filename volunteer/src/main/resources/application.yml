server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/volunteer_service?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    password: 111111
    username: root
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis-plus:
  type-aliases-package: com.volunteer.entity
#  mapper-locations: classpath:mapper/*.xml
